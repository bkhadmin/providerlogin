services:

  # ─── MySQL Database ──────────────────────────────────────────
  db:
    image: mysql:8.0
    container_name: providerlogin-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-rootpassword}
      MYSQL_DATABASE: providerlogin
      MYSQL_USER: ${DB_USER:-pluser}
      MYSQL_PASSWORD: ${DB_PASSWORD:-plpassword}
    volumes:
      - db_data:/var/lib/mysql
      - ./docker/init:/docker-entrypoint-initdb.d:ro
    ports:
      - "3307:3306"          # ใช้ 3307 เพราะ 3306 ถูก XAMPP MySQL ใช้อยู่
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${DB_ROOT_PASSWORD:-rootpassword}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - app-net

  # ─── Node.js App ─────────────────────────────────────────────
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: providerlogin-app
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - .env
    environment:
      DB_HOST: db
      DB_PORT: 3306
      DB_USER: ${DB_USER:-pluser}
      DB_PASSWORD: ${DB_PASSWORD:-plpassword}
      DB_NAME: providerlogin
    ports:
      - "${APP_PORT:-3000}:3000"
    networks:
      - app-net

  # ─── Adminer (DB GUI) ────────────────────────────────────────
  adminer:
    image: adminer:latest
    container_name: providerlogin-adminer
    restart: unless-stopped
    depends_on:
      - db
    ports:
      - "${ADMINER_PORT:-8080}:8080"
    environment:
      ADMINER_DEFAULT_SERVER: db
    networks:
      - app-net

volumes:
  db_data:
    driver: local

networks:
  app-net:
    driver: bridge
